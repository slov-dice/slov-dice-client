import { motion } from 'framer-motion'

import { HeroBackground } from 'features/HeroBackground'

export const Lobby = () => {
  return (
    <motion.div>
      LOBBY
      <HeroBackground />
    </motion.div>
  )
}
